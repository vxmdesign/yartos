#
# For a description of the syntax of this configuration file,
# see misc/tools/kconfig-language.txt.
#

config LIBNETAPP_TFTPC
	bool "TFTP client"
	default n
	---help---
		Enable support for the TFTP client.

if LIBNETAPP_TFTPC
endif
