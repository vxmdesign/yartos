#
# For a description of the syntax of this configuration file,
# see misc/tools/kconfig-language.txt.
#

config LIBNETAPP_TELNETD
	bool "Telet daemon"
	default n
	depends on NET && NET_TCP
	---help---
		Enable support for the Telnet daemon.

if LIBNETAPP_TELNETD
endif
