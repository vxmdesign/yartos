#
# For a description of the syntax of this configuration file,
# see misc/tools/kconfig-language.txt.
#

config LIBNETAPP_FTPD
	bool "FTP server"
	default n
	---help---
		Enable support for the FTP server.

if LIBNETAPP_FTPD
endif
